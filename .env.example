# Vercel KV (Upstash Redis) — create in Vercel Dashboard → Storage
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Vercel Blob — create in Vercel Dashboard → Storage
BLOB_READ_WRITE_TOKEN=

# Local dev: run `npm run dev:vercel` in one terminal and `npm run dev` in another
# (vite proxies /api to port 3000)
